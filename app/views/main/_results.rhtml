<% if @result %>
    <h3>Result for <%= @req_method.blank? ? "" : @req_method + " " %>'<%= (API_PATH + @query).gsub('?', '&').sub('&', '?') %>'</h3>
    <pre>
        <%= h @result.body %>
    </pre>
<% end %>
